# PizzaLimbo runtime configuration template.
# Supply database credentials only in the ignored runtime config; never commit them here.
db:
  host: "127.0.0.1"
  port: 3306
  name: "pizzasmp"
  user: ""
  password: ""
smp:
  probe-host: "127.0.0.1"
  probe-port: 25566
  server-name: "smp"
  return-grace-seconds: 45
messages:
  chat: "&bMaintenance is in progress. You will return automatically."
  hotbar: "&7Maintenance in progress. Returning shortly."
